.benefits-heading.medium-12.columns Identity Theft 911
%ul.large-block-grid-2.small-block-grid-1
  - @identitytheft_benefits.each do |benefit|
    %li.benefit
      .inner
        .flip
          .card
            .face.front
              .inner
                .reward-title= benefit.title
            .face.back
              .inner
                .reward-title= benefit.content.html_safe
.benefits-heading.medium-12.columns Life Steps
%ul.large-block-grid-2.small-block-grid-1
  - @lifestep_benefits.each do |benefit|
    %li.benefit
      .inner
        .flip
          .card
            .face.front
              .inner
                .reward-title= benefit.title
            .face.back
              .inner
                .reward-title= benefit.content.html_safe
.benefits-heading.medium-12.columns Lifelock
%ul.large-block-grid-2.small-block-grid-1
  - @lifelock_benefits.each do |benefit|
    %li.benefit
      .inner
        .flip
          .card
            .face.front
              .inner
                .reward-title= benefit.title
            .face.back
              .inner
                .reward-title= benefit.content.html_safe
